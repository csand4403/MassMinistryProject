# SESSION SUMMARY

Started the auth, role-based access, and minister self-service foundation. Step 1 is drafted as a new numbered migration: `supabase/migrations/013_auth_roles_and_assignment_status.sql`.

The migration adds the `app_user_role` enum, creates `public.app_user` linked to `auth.users`, `parish`, and optional `minister`, adds indexes including one auth user per linked minister, preserves the existing `assignment.status` usage, and expands assignment responses to include `PENDING` and `DECLINED`.

After an attempted Supabase run failed with `22P02 invalid input value for enum assignment_status: "PENDING"`, the migration was repaired to support the deployed enum-backed `assignment.status` shape. It now adds `PENDING` and `DECLINED` to `public.assignment_status` when that enum exists, and only creates a text check constraint when `assignment.status` is actually text.

Work is intentionally paused for migration review/application approval before continuing into login, route protection, role redirects, and `/my-schedule`.

# STATUS OF STEPS 1-8

1. SCHEMA: Partial. Migration file written and repaired after the enum status error. Needs to be rerun in Supabase.
2. SUPABASE AUTH: Not started.
3. INITIAL ADMIN BOOTSTRAP: Not started. Needs user confirmation of approach before implementation. Recommended approach: print SQL to create the first `app_user` row for `chases@maryimmaculatechurch.org` after the auth user exists.
4. ROLE-BASED ACCESS: Not started.
5. PROTECT ROUTES: Not started.
6. MY SCHEDULE VIEW: Not started.
7. EMAIL CONFIRM/DECLINE: Not started.
8. USERS & ROLES SETTINGS: Not started.

# MANUAL STEPS NEEDED

Rerun `supabase/migrations/013_auth_roles_and_assignment_status.sql` in Supabase SQL Editor.

After the migration is approved/applied, confirm which admin bootstrap approach to use:

- SQL bootstrap for `chases@maryimmaculatechurch.org` after creating/signing up that auth user.
- One-time setup page that only works while zero admins exist.

Supabase email/password auth will also need to be enabled in the Supabase dashboard when Step 2 begins.

# ISSUES FOUND

`supabase/migrations/001_initial_schema.sql` is empty in git, so the migration was written defensively against the live schema implied by existing code and later migrations.

There were pre-existing unrelated local report changes before this task. They were not edited for this migration milestone and should not be included in the Step 1 commit.

The first Supabase run failed because the live database uses an `assignment_status` enum for `assignment.status`; the migration originally tried to add a text check containing enum values that did not exist yet. This has been fixed in the migration.

Browser verification was not performed for Step 1 because the repaired migration has not been successfully applied yet. Verification should happen after the user confirms the migration was applied.

# NEXT STEP

Have the user review and apply `supabase/migrations/013_auth_roles_and_assignment_status.sql` in Supabase. Once confirmed, implement Step 2 with email/password login, then Step 3 using the user-confirmed bootstrap approach.

# IDEAS PARKED

For Step 7, consider adding a separate `assignment_response_token` table instead of storing raw tokens on `assignment`, so confirm/decline links can be single-purpose, expirable, and auditable without changing assignment identity.
